import React from 'react';

export default function About(){
    return (
        <div className="card">
            <div className="card-body">
                <h2 className="card-title">À propos</h2>
                <p className="card-text">Cet exemple montre comment créer une application monopage (SPA) avec Laravel (Vite) et React. Le routage se gère côté client avec React Router.</p>
            </div>
        </div>
    );
}
